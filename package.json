{
  "name": "full-time-equivalent",
  "private": true,
  "version": "0.1.0",
  "description": "A people operations tower defence. You do not place people. You place processes.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run build -w @fte/shared && concurrently -n server,client -c magenta,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "FTE_PORT=8787 npm run dev -w @fte/server",
    "dev:client": "npm run dev -w @fte/client -- --port 5173 --strictPort",
    "build": "npm run build -w @fte/shared && npm run build -w @fte/server && npm run build -w @fte/client",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b packages/shared packages/server packages/client",
    "balance": "npm run build -w @fte/shared && node packages/shared/dist/tools/balance.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
